# Codeception Test Suite Configuration

# suite for unit (internal) tests.
class_name: AppApiTester
modules:
    enabled: [PhpBrowser,REST,ApiHelper]
    config:
      PhpBrowser:
        url: http://localhost:8000/
      REST:
        url: http://localhost:8000/app/
